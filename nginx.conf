events {
    worker_connections 300;
}

http {
    upstream api {
        server java_api_1:8080;
        server java_api_2:8080;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api;
        }
    }
}