/*
package lipe.dev.bank.entities.seeds;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Component;

//@Component
public class ClienteSeed {

    @Autowired
    private JdbcTemplate jdbcTemplate;

    public void insertData() {

        jdbcTemplate.execute("INSERT INTO cliente(id, saldo, limite, versao) VALUES (1, 0, 100000, 0);");
        jdbcTemplate.execute("INSERT INTO cliente(id, saldo, limite, versao) VALUES (2, 0, 80000, 0);");
        jdbcTemplate.execute("INSERT INTO cliente(id, saldo, limite, versao) VALUES (3, 0, 1000000, 0);");
        jdbcTemplate.execute("INSERT INTO cliente(id, saldo, limite, versao) VALUES (4, 0, 10000000, 0);");
        jdbcTemplate.execute("INSERT INTO cliente(id, saldo, limite, versao) VALUES (5, 0, 500000, 0);");

    }
}*/